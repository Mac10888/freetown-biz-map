# All secrets must be set via deployment environment or a secrets manager.
# Do NOT commit secrets to this file or version control.
# Example (do not use real values):
# SUPABASE_URL=your-supabase-url
# SUPABASE_ANON_KEY=your-anon-key
# MAPBOX_TOKEN=your-mapbox-token
